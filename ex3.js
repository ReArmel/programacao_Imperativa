let somar = function (numA, numB) {
    console.log(numA + numB);
    }
    somar(13, 27);
    somar(25, 38);
 
    let x = 42;
    let y = 25;
    console.log(x + y);
    let z = 13;
    console.log((x + y) + z);

    console.log(x + 1);

    let palavra = "olá";
    console.log(palavra + 23);

  let nome = "Marina" 
  let sobrenome = "Santana"
  let frase = "está estudando programação"
  console.log( nome + " " + sobrenome + " " + frase);
  
    